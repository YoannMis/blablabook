server {
    listen 80;
    server_name yoannmis-server.eddi.cloud www.yoannmis-server.eddi.cloud;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}