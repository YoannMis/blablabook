server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
    }

    location / {
        return 200 "Certbot validation server. This is temporary.";
        add_header Content-Type text/plain;
    }
}